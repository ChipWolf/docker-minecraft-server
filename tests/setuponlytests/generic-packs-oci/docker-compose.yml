services:
  mc:
    image: ${IMAGE_TO_TEST:-itzg/minecraft-server}
    environment:
      EULA: "true"
      SETUP_ONLY: "true"
      # Public OCI artifact: shared base layer + per-pack overlay layer.
      # Source: https://github.com/ChipWolf/oci-modpack-demo
      GENERIC_PACKS: oci://ghcr.io/chipwolf/oci-modpack-demo/tech:latest
      LOG_TIMESTAMP: "true"
      # the following are only used to speed up test execution
      TYPE: CUSTOM
      CUSTOM_SERVER: /servers/fake.jar
      VERSION: 1.18.1
      DEBUG: "true"
    volumes:
      - ./data:/data
      - ./fake.jar:/servers/fake.jar
      # Bind a local mc-image-helper install (see generic-packs-oci/require.sh).
      # require.sh skips this test when MC_HELPER_LOCAL is unset (upstream CI).
      - ${MC_HELPER_LOCAL}:/usr/share/mc-image-helper:ro
